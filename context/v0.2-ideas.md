# V0.2 — Ideas y Próximos Pasos

## 1. PWA real (instalación + offline)
- `manifest.json` + service worker → instalar en home screen del celular
- Cachear datos clave en localStorage con sincronización diferida

## 2. Precios por temporada
- Columnas `precio_temporada_alta` y `precio_temporada_baja` en `inmuebles`
- Auto-selección según fechas al crear reserva (ej. Dic–Mar = alta, resto = baja)

## 3. Gestión de limpieza ("chica del recambio")
- Calendario de limpiezas: checklist por inmueble entre reservas
- Historial de limpiezas linkeado al gasto de recambio

## 4. Exportación / Reportes
- Exportar dashboard financiero a CSV/PDF
- Balance anual imprimible por inmueble

## 5. Mejoras de UX pendientes
- Skeleton loaders en vez de "Cargando..."
- Toast notifications unificadas (feedback de acciones)
- Pull-to-refresh en vistas principales
- Swipe actions en tabla de Alquileres

## 6. Sincronizar Textos Pre-armados
- Migrar de localStorage a Supabase (tabla `plantillas_texto`)

## 7. Deuda técnica
- Reemplazar `var` por `const`/`let` en views pendientes
- Extraer lógica repetitiva a hooks custom (`useSenias`, `useFetchAlquileres`)
- Lazy loading de rutas con `React.lazy`

## 8. Auditoría / Backup
- Tabla `auditoria` con log de cambios en alquileres
- Botón "Exportar todo" (JSON de todas las tablas)
